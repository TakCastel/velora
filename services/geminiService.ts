// Le service d'IA a été désactivé et le code supprimé.
export interface ParsedSubscription {
  name: string;
  price: number;
  day: number;
  category: string;
}

export const parseSubscriptionWithAI = async (text: string): Promise<ParsedSubscription | null> => {
  return null;
};